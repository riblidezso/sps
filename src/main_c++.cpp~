#include <cb_data.h>

int main()
{
	cb_data model;

	model.read_buff_atof();

	model.get_data_from_model();

	model.dust();

	model.get_age();

	model.get_sfr();

	model.conv_to_age_vector();	

	model.write_convresult();

	return 0;
}
